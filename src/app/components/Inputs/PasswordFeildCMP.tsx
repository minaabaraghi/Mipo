import { TextField } from "@mui/material";

export default function PasswordFieldCMP() {
  return (
    <TextField
      label="Size"
      id="filled-size-normal"
      defaultValue="Normal"
      variant="filled"
      type="password"
    />
  );
}
